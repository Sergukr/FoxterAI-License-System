//+------------------------------------------------------------------+
//|                                                     FoxterAI.mq4 |
//|                                    Торговый робот с ATR-трендом  |
//|                                                    Версия 1.6    |
//+------------------------------------------------------------------+
#property copyright "FoxterX"
#property version   "1.60"
#property strict

//--- Подключаем все модули
#include "../FoxterAI_modules/1_enums.mqh"
#include "../FoxterAI_modules/2_inputs.mqh"
#include "../FoxterAI_modules/3_globals.mqh"
#include "../FoxterAI_modules/4_series_functions.mqh"
#include "../FoxterAI_modules/5_trend_functions.mqh"
#include "../FoxterAI_modules/6_filter_functions.mqh"
#include "../FoxterAI_modules/7_trading_functions.mqh"
#include "../FoxterAI_modules/8_position_management.mqh"
#include "../FoxterAI_modules/9_panel_create.mqh"
#include "../FoxterAI_modules/10_panel_update.mqh"
#include "../FoxterAI_modules/11_panel_handlers.mqh"
#include "../FoxterAI_modules/12_visualization.mqh"
#include "../FoxterAI_modules/13_license.mqh"
#include "../FoxterAI_modules/14_license_dialog.mqh"

//+------------------------------------------------------------------+
//| ИНИЦИАЛИЗАЦИЯ СОВЕТНИКА                                          |
//+------------------------------------------------------------------+
int OnInit() {
    Print("========================================");
    Print("    ЗАПУСК FOXTERAI v1.6");
    Print("========================================");
    Print("Счет: ", AccountNumber());
    Print("Брокер: ", AccountCompany());
    Print("Баланс: ", AccountBalance());
    Print("Символ: ", Symbol());
    Print("========================================");
    
    //--- Проверка режима тестера
    if(IsTestMode()) {
        Print("🔧 РЕЖИМ ТЕСТЕРА СТРАТЕГИЙ АКТИВЕН");
        Print("Лицензия автоматически валидна в тестере");
    }
    
    //--- Включаем события графика для диалога (только не в тестере)
    if(!IsTestMode()) {
        ChartSetInteger(0, CHART_EVENT_MOUSE_MOVE, true);
        ChartSetInteger(0, CHART_EVENT_OBJECT_CREATE, true);
        ChartSetInteger(0, CHART_EVENT_OBJECT_DELETE, true);
    }
    
    //--- Проверка и получение лицензионного ключа
    string licenseKey = GetSavedLicenseKey();
    
    //--- Если ключа нет и НЕ тестер - показываем диалог для ввода
    if(StringLen(licenseKey) == 0 && !IsTestMode()) {
        Print("❗ Лицензионный ключ не найден");
        Print("➜ Открываем диалог для ввода ключа...");
        
        ShowLicenseDialog();
        
        // НЕ продолжаем инициализацию пока не введен ключ
        g_BotEnabled = false;
        
        // Но возвращаем успех чтобы советник работал и ждал ввода ключа
        return(INIT_SUCCEEDED);
    }
    
    //--- Если ключ найден или тестер - проверяем
    if(IsTestMode()) {
        Print("✓ Тестовый режим - используем тестовую лицензию");
    } else {
        Print("✓ Найден сохраненный ключ: ", licenseKey);
        Print("➜ Проверка лицензии на сервере...");
    }
    
    if(!InitializeLicense(licenseKey)) {
        // В тестере это никогда не произойдет
        Print("❌ ОШИБКА: ", g_LicenseMessage);
        Alert("Лицензия недействительна!\n\n", g_LicenseMessage, "\n\nПожалуйста, введите новый ключ.");
        
        // Очищаем старые данные
        ClearAllLicenseData();
        
        // Показываем диалог для ввода нового ключа
        ShowLicenseDialog();
        
        // Блокируем торговлю
        g_BotEnabled = false;
        
        return(INIT_SUCCEEDED);
    }
    
    Print("✅ Лицензия активна: ", GetLicenseStatusText());
    
    //--- Инициализация переменных из входных параметров
    g_UseImpulse = UseImpulse;
    g_FirstLot = FirstLot;
    g_MaxOrdersBuy = MaxOrdersBuy;
    g_MaxOrdersSell = MaxOrdersSell;
    g_MartinCoeff = MartinCoeff;
    g_BasketProfitPercent = BasketProfitPercent;
    g_GridStepPercent = Grid_StepPercent;
    g_BasketType = BasketType;
    g_BasketAfterN = BasketAfterNOrders;
    
    // Инициализация параметров учета издержек
    g_IncludeCommissionSwap = IncludeCommissionSwap;
    g_EstimatedCommissionPerLot = EstimatedCommissionPerLot;
    g_SlippagePercent = SlippagePercent;
    
    // Устанавливаем позицию панели из входных параметров
    g_PanelPosX = PanelX;
    g_PanelPosY = PanelY;
    
    Print("Позиция панели: X=", g_PanelPosX, " Y=", g_PanelPosY);
    
    //--- Проверка корректности параметров
    if(g_FirstLot < MarketInfo(Symbol(), MODE_MINLOT)) {
        Alert("Начальный лот меньше минимального!");
        return(INIT_PARAMETERS_INCORRECT);
    }
    
    //--- Инициализация структур серий
    ResetSeriesInfo(g_BuySeries);
    ResetSeriesInfo(g_SellSeries);
    
    //--- Загрузка сохраненных настроек
    LoadSettings();
    
    //--- Определение начального тренда
    DetectInitialTrend();
    
    //--- Создание интерактивной панели управления
    if(ShowPanel) {
        Print("Создание панели управления...");
        CreateInteractivePanel();
        if(!IsTestMode()) {
            ChartSetInteger(0, CHART_EVENT_MOUSE_MOVE, true);
        }
    } else {
        Print("Панель управления отключена в настройках");
    }
    
    Print("========================================");
    Print("✅ СОВЕТНИК ГОТОВ К РАБОТЕ");
    Print("Робот: ", g_BotEnabled ? "ВКЛЮЧЕН" : "ВЫКЛЮЧЕН (нажмите START на панели)");
    Print("Импульс: ", g_UseImpulse ? "ВКЛЮЧЕН (ждем импульс)" : "ВЫКЛЮЧЕН (вход по тренду)");
    Print("Тренд при старте: ", g_TrendState == TREND_BUY ? "BUY ↑" : 
          (g_TrendState == TREND_SELL ? "SELL ↓" : "НЕ ОПРЕДЕЛЕН"));
    if(IsTestMode()) {
        Print("🔧 РЕЖИМ ТЕСТЕРА: Лицензия не проверяется");
    } else {
        Print("📅 Проверка лицензии: раз в сутки");
        Print("💓 Heartbeat: раз в 12 часов");
    }
    Print("========================================");
    
    return(INIT_SUCCEEDED);
}

//+------------------------------------------------------------------+
//| ДЕИНИЦИАЛИЗАЦИЯ                                                  |
//+------------------------------------------------------------------+
void OnDeinit(const int reason) {
    //--- Сохранение настроек (не в тестере)
    if(!IsTestMode()) {
        SaveSettings();
    }
    
    //--- Удаление панели
    DeletePanel();
    
    //--- Удаление диалога лицензии если открыт
    if(g_LicenseDialogOpen) {
        CloseLicenseDialog();
    }
    
    //--- Удаление визуальных объектов
    DeleteVisualObjects();
    
    string reasonText = "";
    switch(reason) {
        case REASON_PROGRAM:     reasonText = "Советник остановлен пользователем"; break;
        case REASON_REMOVE:      reasonText = "Советник удален с графика"; break;
        case REASON_RECOMPILE:   reasonText = "Перекомпиляция"; break;
        case REASON_CHARTCHANGE: reasonText = "Изменение символа или периода"; break;
        case REASON_CHARTCLOSE:  reasonText = "График закрыт"; break;
        case REASON_PARAMETERS:  reasonText = "Изменение параметров"; break;
        case REASON_ACCOUNT:     reasonText = "Смена счета"; break;
        default:                 reasonText = "Неизвестная причина";
    }
    
    Print("========================================");
    Print("СОВЕТНИК ОСТАНОВЛЕН");
    Print("Причина: ", reasonText);
    Print("========================================");
}

//+------------------------------------------------------------------+
//| ОСНОВНАЯ ФУНКЦИЯ ТИКА - ПОЛНАЯ ВЕРСИЯ                           |
//+------------------------------------------------------------------+
void OnTick() {
    //--- В ТЕСТЕРЕ НЕ ПРОВЕРЯЕМ ДИАЛОГ И ЛИЦЕНЗИЮ
    if(!IsTestMode()) {
        //--- Если диалог лицензии открыт - ждем ввода ключа
        if(g_LicenseDialogOpen) {
            return; // Не торгуем пока не введен ключ
        }
        
        //--- Проверка лицензии РАЗ В СУТКИ (НЕ В ТЕСТЕРЕ!)
        static datetime lastLicenseCheck = 0;
        if(TimeCurrent() - lastLicenseCheck > 86400) { // РАЗ В СУТКИ (24 часа)
            lastLicenseCheck = TimeCurrent();
            
            Print("📅 Ежедневная проверка лицензии...");
            
            if(!CheckLicense()) {
                // Если лицензия стала недействительной
                g_BotEnabled = false; // Останавливаем робота
                
                // Если ключ удален с сервера - показываем диалог для нового ключа
                if(g_LicenseState == LICENSE_INVALID && 
                   (StringFind(g_LicenseMessage, "удален") >= 0 || StringFind(g_LicenseMessage, "не найден") >= 0)) {
                    Alert("Лицензия удалена с сервера!\nВведите новый ключ.");
                    ClearAllLicenseData(); // Очищаем старые данные
                    ShowLicenseDialog();   // Показываем диалог для нового ключа
                } else {
                    Alert("❌ Лицензия недействительна! Торговля остановлена.\n" + g_LicenseMessage);
                }
                
                Print("❌ Лицензия недействительна: ", g_LicenseMessage);
            } else {
                Print("✅ Лицензия проверена: ", GetLicenseStatusText());
            }
        }
        
        //--- Отправляем heartbeat раз в 12 ЧАСОВ (НЕ В ТЕСТЕРЕ!)
        static datetime lastHeartbeat = 0;
        if(TimeCurrent() - lastHeartbeat > 43200) { // 12 ЧАСОВ
            lastHeartbeat = TimeCurrent();
            if(IsLicenseValidForTrading()) {
                SendHeartbeat();
            }
        }
    }
    
    //--- Сбрасываем флаг клика на кнопку
    g_ButtonClicked = false;
    
    //--- Проверка, включен ли робот И лицензия валидна
    if(!g_BotEnabled || !IsLicenseValidForTrading()) {
        if(ShowPanel) UpdateInteractivePanel();
        
        //--- При невалидной лицензии можем только закрывать позиции
        if(!IsLicenseValidForTrading() && (g_BuySeries.active || g_SellSeries.active)) {
            ManagePositions(); // Только управление TP для закрытия существующих позиций
        }
        return;
    }
    
    //--- Обновление состояния серий
    UpdateSeriesInfo();
    
    //--- Обновление статистики
    UpdateStatistics();
    
    //--- ШВЕЙЦАРСКАЯ ЛОГИКА ВХОДА В РЫНОК
    //--- Разделяем логику для режимов с импульсом и без
    
    if(g_UseImpulse) {
        //========================================
        // РЕЖИМ С ИМПУЛЬСОМ - работаем по барам
        //========================================
        
        //--- Проверка нового бара на таймфрейме тренда
        static datetime lastTrendBar = 0;
        datetime currentTrendBar = iTime(Symbol(), TrendTimeframe, 0);
        if(lastTrendBar != currentTrendBar) {
            lastTrendBar = currentTrendBar;
            
            //--- Обновление тренда
            UpdateTrend();
        }
        
        //--- Проверка нового бара на таймфрейме импульса
        static datetime lastImpulseBar = 0;
        datetime currentImpulseBar = iTime(Symbol(), ImpulseTimeframe, 0);
        if(lastImpulseBar != currentImpulseBar) {
            lastImpulseBar = currentImpulseBar;
            
            //--- Поиск импульсного сигнала
            CheckImpulseSignal();
        }
    }
    else {
        //========================================
        // РЕЖИМ БЕЗ ИМПУЛЬСА - работаем по тикам
        //========================================
        
        //--- Обновление тренда только на новом баре
        static datetime lastTrendBarNoImpulse = 0;
        datetime currentTrendBarNoImpulse = iTime(Symbol(), TrendTimeframe, 0);
        if(lastTrendBarNoImpulse != currentTrendBarNoImpulse) {
            lastTrendBarNoImpulse = currentTrendBarNoImpulse;
            
            //--- Обновление тренда
            UpdateTrend();
        }
        
        //--- ПРОВЕРКА ВХОДА НА КАЖДОМ ТИКЕ (без импульса)
        CheckEntryWithoutImpulse();
    }
    
    //--- Управление существующими позициями (всегда на каждом тике)
    ManagePositions();
    
    //--- Обновление визуализации TP
    if(ShowVisualObjects && ShowTPLevels) {
        DrawTPLevels();
    }
    
    //--- Обновление панели
    if(ShowPanel) {
        UpdateInteractivePanel();
    }
}

//+------------------------------------------------------------------+
//| ПРОВЕРКА ВХОДА БЕЗ ИМПУЛЬСА - ПОЛНАЯ ВЕРСИЯ                    |
//+------------------------------------------------------------------+
void CheckEntryWithoutImpulse() {
    //--- Эта функция вызывается на КАЖДОМ тике когда импульс выключен
    
    //--- Проверяем направление торговли
    bool canBuy = (g_TradeDirection == TRADE_BOTH || g_TradeDirection == TRADE_BUY_ONLY);
    bool canSell = (g_TradeDirection == TRADE_BOTH || g_TradeDirection == TRADE_SELL_ONLY);
    
    //--- Проверка для BUY
    if(g_TrendState == TREND_BUY && canBuy) {
        // Проверяем физическое наличие ордеров
        if(!HasActiveOrders(OP_BUY)) {
            //--- Проверяем ВСЕ фильтры включая спред
            if(CheckFilters()) {
                Print("💫 Вход по тренду BUY (импульс ВЫКЛ) - открываем!");
                OpenPosition(OP_BUY);
            } else {
                //--- Логируем причину блокировки раз в минуту
                static datetime lastLogBuy = 0;
                if(TimeCurrent() - lastLogBuy > 60) {
                    lastLogBuy = TimeCurrent();
                    double currentSpread = MarketInfo(Symbol(), MODE_SPREAD);
                    double marginLevel = AccountInfoDouble(ACCOUNT_MARGIN_LEVEL);
                    Print("⏸ BUY вход заблокирован. Спред: ", currentSpread, "/", MaxSpread, 
                          " пунктов, Уровень маржи: ", marginLevel, "%");
                }
            }
        }
    }
    
    //--- Проверка для SELL
    if(g_TrendState == TREND_SELL && canSell) {
        // Проверяем физическое наличие ордеров
        if(!HasActiveOrders(OP_SELL)) {
            //--- Проверяем ВСЕ фильтры включая спред
            if(CheckFilters()) {
                Print("💫 Вход по тренду SELL (импульс ВЫКЛ) - открываем!");
                OpenPosition(OP_SELL);
            } else {
                //--- Логируем причину блокировки раз в минуту
                static datetime lastLogSell = 0;
                if(TimeCurrent() - lastLogSell > 60) {
                    lastLogSell = TimeCurrent();
                    double currentSpread = MarketInfo(Symbol(), MODE_SPREAD);
                    double marginLevel = AccountInfoDouble(ACCOUNT_MARGIN_LEVEL);
                    Print("⏸ SELL вход заблокирован. Спред: ", currentSpread, "/", MaxSpread, 
                          " пунктов, Уровень маржи: ", marginLevel, "%");
                }
            }
        }
    }
}

//+------------------------------------------------------------------+
//| ОБРАБОТКА СОБЫТИЙ ГРАФИКА - ПОЛНАЯ ВЕРСИЯ                       |
//+------------------------------------------------------------------+
void OnChartEvent(const int id, const long &lparam, const double &dparam, const string &sparam) {
    //--- В тестере не обрабатываем события
    if(IsTestMode()) return;
    
    //--- Обработка диалога лицензии (приоритет)
    if(g_LicenseDialogOpen) {
        if(id == CHARTEVENT_OBJECT_CLICK) {
            if(HandleLicenseDialogClick(sparam) == true) {
                //--- Если ключ был введен, проверяем его
                if(StringLen(g_EnteredLicenseKey) > 0) {
                    Print("Лицензионный ключ введен, проверяем...");
                    
                    // Пытаемся инициализировать с новым ключом
                    if(InitializeLicense(g_EnteredLicenseKey)) {
                        Print("✅ Лицензия активирована успешно!");
                        Alert("Лицензия активирована!\n\nСоветник готов к работе.");
                        
                        // Продолжаем нормальную инициализацию после успешной активации
                        g_BotEnabled = true;
                        LoadSettings();
                        DetectInitialTrend();
                        if(ShowPanel) {
                            CreateInteractivePanel();
                        }
                    } else {
                        Alert("Ошибка активации лицензии:\n\n" + g_LicenseMessage);
                        Print("❌ Ошибка активации: ", g_LicenseMessage);
                        
                        // Если ключ не найден на сервере - даем возможность ввести новый
                        ShowLicenseDialog();
                    }
                }
                return;
            }
        }
        return; // Блокируем другие события пока открыт диалог
    }
    
    //--- Обработка нажатия кнопок панели
    if(id == CHARTEVENT_OBJECT_CLICK) {
        if(StringFind(sparam, PANEL_PREFIX) == 0) {
            HandleButtonClick(sparam);
            ChartRedraw();
            return; // Важно: выходим после обработки клика
        }
    }
    
    //--- Обработка перетаскивания панели
    if(id == CHARTEVENT_MOUSE_MOVE) {
        int mouseX = (int)lparam;
        int mouseY = (int)dparam;
        int mouseState = (int)StringToInteger(sparam);
        
        //--- Если был клик на кнопку, не начинаем перетаскивание
        if(g_ButtonClicked) {
            g_PanelDragging = false;
            return;
        }
        
        //--- Проверяем, не находится ли мышь над кнопкой
        bool mouseOverButton = false;
        for(int i = 0; i < ObjectsTotal(); i++) {
            string objName = ObjectName(i);
            if(StringFind(objName, PANEL_PREFIX + "Btn") == 0 || 
               StringFind(objName, PANEL_PREFIX + "Edit") == 0) {
                // Получаем позицию и размеры объекта
                int objX = (int)ObjectGetInteger(0, objName, OBJPROP_XDISTANCE);
                int objY = (int)ObjectGetInteger(0, objName, OBJPROP_YDISTANCE);
                int objWidth = (int)ObjectGetInteger(0, objName, OBJPROP_XSIZE);
                int objHeight = (int)ObjectGetInteger(0, objName, OBJPROP_YSIZE);
                
                if(mouseX >= objX && mouseX <= objX + objWidth &&
                   mouseY >= objY && mouseY <= objY + objHeight) {
                    mouseOverButton = true;
                    break;
                }
            }
        }
        
        //--- Если левая кнопка мыши нажата
        if((mouseState & 1) != 0) {
            if(!g_PanelDragging) {
                //--- Начинаем перетаскивание если мышь над панелью, но НЕ над кнопкой
                if(IsMouseOverPanel(mouseX, mouseY) && !mouseOverButton) {
                    g_PanelDragging = true;
                    g_DragOffsetX = mouseX - g_PanelPosX;
                    g_DragOffsetY = mouseY - g_PanelPosY;
                }
            } else {
                //--- Перемещаем панель
                int newX = mouseX - g_DragOffsetX;
                int newY = mouseY - g_DragOffsetY;
                
                //--- Ограничиваем движение панели в пределах графика
                newX = MathMax(0, newX);
                newY = MathMax(0, newY);
                
                MovePanel(newX, newY);
            }
        } else {
            //--- Отпустили кнопку мыши
            g_PanelDragging = false;
        }
    }
}